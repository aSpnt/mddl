package ru.softmachine.odyssey.backend.cms.dto.filter;

/**
 * Глобальная операция объединения условий фильтров (по-умолчанию подразумевается AND)
 */
public enum FilterGlobalOperator {

    OR,
    AND,
}
